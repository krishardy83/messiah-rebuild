---
import type { HTMLAttributes } from "astro/types";
import { cn } from "../lib/utils";

interface Props extends HTMLAttributes<"img"> {
  caption: string;
  description?: string;
  size?: "small" | "large";
  image: {
    src: string;
    alt: string;
  };
  button?: {
    label: string;
    href: string;
  };
}

const {
  caption,
  size = "large",
  description,
  image,
  button,
  class: className,
  ...props
} = Astro.props;
---

<figure
  data-analytics="image-hover-link"
  class={cn("relative max-w-max not-prose group", className)}
  tabindex="0"
>
  <img {...props} src={image.src} alt={image.alt} class="w-full h-full object-cover" />

  <figcaption
    class={cn(
      "absolute left-0 top-0 duration-300 group-hover:w-full group-hover:h-full group-focus:w-full group-focus:h-full group-focus-within:w-full group-focus-within:h-full transition-all bg-primary-800/80 text-center group-hover:overflow-y-auto group-focus:overflow-y-auto group-focus-within:overflow-y-auto",
      {
        "group-hover:p-8 group-focus:p-8 group-focus-within:p-8 h-12 w-1/2": size === "large",
        "group-hover:p-4 group-focus:p-4 group-focus-within:p-4 h-11 w-full": size === "small",
      }
    )}
  >
    <h4
      class={cn("font-title font-bold uppercase w-max mx-auto text-white py-2", {
        "text-2xl px-12": size === "large",
        "text-lg px-4": size === "small",
      })}
    >
      {caption}
    </h4>

    {
      description ? (
        <p
          class={cn(
            "text-white mb-4 border-t border-white pt-2 opacity-0 group-hover:opacity-100 group-focus:opacity-100 group-focus-within:opacity-100 transition-opacity",
            {
              "text-sm": size === "small",
            }
          )}
        >
          {description}
        </p>
      ) : null
    }

    {
      button ? (
        <a
          class="btn-quaternary w-max mx-auto opacity-0 group-hover:opacity-100 group-focus:opacity-100 group-focus-within:opacity-100"
          href={button.href}
        >
          {button.label}
        </a>
      ) : null
    }
  </figcaption>
</figure>
